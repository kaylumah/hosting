<?xml version="1.0" encoding="utf-8"?>
<Project>
  <Import Project="../Directory.Build.props" />

  <PropertyGroup>
    <ManagePackageVersionsCentrally Condition="$(MSBuildProjectName.EndsWith('Hosting'))">true</ManagePackageVersionsCentrally>
  </PropertyGroup>

</Project>