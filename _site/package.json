{
  "name": "kaylumah_site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i styles.css -o compiled.css",
    "build:prod": "npx tailwindcss -i styles.css -o compiled.css --minify",
    "http-server": "npx http-server",
    "swa": "swa start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@azure/static-web-apps-cli": "1.0.1",
    "@tailwindcss/line-clamp": "0.3.0",
    "@tailwindcss/typography": "0.5.10",
    "tailwindcss": "3.0.0"
  }
}
